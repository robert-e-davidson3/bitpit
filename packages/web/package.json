{
  "name": "@bitpit/web",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "echo 'No tests specified' && exit 0",
    "typecheck": "tsc --noEmit",
    "clean": "run-p clean:*",
    "clean:dist":"rm -rf dist || true",
    "clean:buildinfo":"rm *.tsbuildinfo || true",
    "preview": "vite preview"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Web client for BitPit",
  "dependencies": {
    "@bitpit/common": "file:../common",
    "@lit/context": "^1.1.5",
    "lit": "^3.3.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.8.3",
    "vite": "^6.2.6",
    "vite-plugin-checker": "^0.9.1"
  }
}
